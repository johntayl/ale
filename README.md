ALE - Angular Level Editor
==================================

ALE is a simple level editor that is built on top of the AngularJS framework.
The backend consists of PHP scripts for reading/writing files.
